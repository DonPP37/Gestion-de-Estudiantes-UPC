<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IntercambioAcademicoUPC">

        <activity android:name=".LoginActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".PerfilActivity" android:exported="false" />

        <!-- HU-05 - HU-06 -->
        <activity android:name=".AdminUsuariosActivity" android:exported="false" />
        <activity android:name=".CrearCursoActivity" android:exported="false" />

        <!-- HU-07 -->
        <activity android:name=".MatriculaEstudianteActivity" android:exported="false" />

        <!-- Aula Virtual y Materiales (Docente/Estudiante/Admin) -->
        <activity android:name=".AdminCursosGeneralActivity" android:exported="false" />
        <activity android:name=".DetalleCursoActivity" android:exported="false" />

    </application>

</manifest>